# State

current_version: v1

## Current Version
- active version은 `v1`이다.
- `v1`의 목표는 `tagging-version` 기반 IPR 릴리즈 후처리 자동화 CLI를 API-only 방식으로 정리하는 것이다.
- 현재 active feature handoff는 없다.
- 완료된 verified plan은 `docs/v1/completed/`에 정리되어 있다.

## Completed Versions
- none

## Notes
- `v1` 구현은 Linux 환경에서 동작해야 하며 브라우저 자동화는 범위에서 제외한다.
- Jira는 REST API와 `ReleaseNote.jspa`를 사용하고, Confluence는 JSON-RPC를 우선 사용한다.
- verified plan은 완료 후 `docs/v1/completed/`로 이동한다.
